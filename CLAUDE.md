# Notes for Claude

- Weekly City Review (WBR) decks in Google Slides: always follow `docs/wbr-deck-style.md`. It holds the approved reference deck, the layout, polarity rules, and the user's standing instructions: no unnecessary or chunked slides, no renames without asking, and check every source before saying data is missing.
- Never commit `google-workspace-access/token.json` or `credentials.json`.
